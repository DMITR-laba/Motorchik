# 🐳 Docker Setup Guide

## Обзор архитектуры

Проект использует Docker Compose для запуска всех сервисов:

1. **PostgreSQL** - основная база данных
2. **Elasticsearch** - поисковый движок для автомобилей
3. **Backend** - FastAPI приложение
4. **Frontend** - Nginx с статическими файлами

## Структура контейнеров

```
┌─────────────┐
│  Frontend   │ (Nginx, порт 3000)
└──────┬──────┘
       │
┌──────▼──────┐
│   Backend   │ (FastAPI, порт 8000)
└──┬───────┬──┘
   │       │
┌──▼──┐ ┌──▼──────────┐
│PostgreSQL│ │Elasticsearch│
│  :5432   │ │   :9200     │
└─────────┘ └─────────────┘
```

## Запуск проекта

### 1. Подготовка

Убедитесь, что у вас есть файл `sqlite/cars.db` в корне проекта:

```bash
sqlite/
  └── cars.db
```

### 2. Запуск всех контейнеров

```bash
docker-compose up -d
```

### 3. Проверка статуса

```bash
docker-compose ps
```

Все сервисы должны быть в статусе `Up` и `healthy`.

## Что происходит при запуске

### 1. PostgreSQL
- Создается база данных `vectordb`
- Данные сохраняются в Docker volume `postgres_data`

### 2. Elasticsearch
- Запускается в single-node режиме
- Данные сохраняются в Docker volume `elasticsearch_data`
- Память ограничена 512MB (можно изменить в docker-compose.yml)

### 3. Backend (автоматическая инициализация)

При первом запуске backend выполняет:

1. **Ожидание сервисов**:
   - Ждет готовности PostgreSQL (healthcheck)
   - Ждет готовности Elasticsearch (healthcheck)

2. **Проверка миграции**:
   - Проверяет, есть ли данные в PostgreSQL
   - Если таблица `cars` пуста, запускает миграцию из SQLite

3. **Миграция из SQLite**:
   - Читает данные из `/app/sqlite/cars.db`
   - Заполняет PostgreSQL таблицами:
     - `cars` (новые автомобили)
     - `used_cars` (подержанные автомобили)
     - `car_options` (опции)
     - `car_options_groups` (группы опций)
     - `car_pictures` (фотографии)
     - `used_car_pictures` (фотографии подержанных)

4. **Настройка Elasticsearch**:
   - Создает индексы для поиска
   - Индексирует данные из PostgreSQL

5. **Запуск приложения**:
   - Запускает FastAPI на порту 8000

## Переменные окружения

### Backend

Основные переменные (устанавливаются в docker-compose.yml):

- `DATABASE_URL` - URL подключения к PostgreSQL
- `POSTGRES_HOST` - хост PostgreSQL (в Docker: `postgres`)
- `POSTGRES_PORT` - порт PostgreSQL (5432)
- `POSTGRES_DB` - имя базы данных (`vectordb`)
- `POSTGRES_USER` - пользователь (`postgres`)
- `POSTGRES_PASSWORD` - пароль (`password`)
- `ELASTICSEARCH_HOST` - хост Elasticsearch (в Docker: `elasticsearch`)
- `ELASTICSEARCH_PORT` - порт Elasticsearch (9200)

Дополнительные переменные можно задать в файле `.env`.

## Volumes

### PostgreSQL
- `postgres_data` - хранит данные PostgreSQL

### Elasticsearch
- `elasticsearch_data` - хранит индексы Elasticsearch

### SQLite (read-only)
- `./sqlite:/app/sqlite:ro` - монтирует папку sqlite для чтения

## Обновление данных

После первоначальной миграции из SQLite, данные обновляются через:

1. **API импорта** (`/api/import/*`) - загрузка новых данных
2. **Парсер** - автоматическая обработка XML файлов
3. **Ручное обновление** - через админ-панель

SQLite файл используется **только для первоначального заполнения**.

## Полезные команды

### Просмотр логов

```bash
# Все сервисы
docker-compose logs -f

# Только backend
docker-compose logs -f backend

# Только PostgreSQL
docker-compose logs -f postgres

# Только Elasticsearch
docker-compose logs -f elasticsearch
```

### Остановка

```bash
docker-compose down
```

### Остановка с удалением volumes (⚠️ удалит все данные!)

```bash
docker-compose down -v
```

### Пересборка контейнеров

```bash
docker-compose build --no-cache
docker-compose up -d
```

### Проверка подключения к PostgreSQL

```bash
docker-compose exec backend psql -h postgres -U postgres -d vectordb
```

### Проверка подключения к Elasticsearch

```bash
curl http://localhost:9200/_cluster/health
```

### Принудительная миграция

Если нужно пересоздать данные:

```bash
# Остановить контейнеры
docker-compose down

# Удалить volume PostgreSQL (⚠️ удалит данные!)
docker volume rm chat-assistant_postgres_data

# Запустить заново
docker-compose up -d
```

## Troubleshooting

### Backend не запускается

1. Проверьте логи: `docker-compose logs backend`
2. Убедитесь, что PostgreSQL и Elasticsearch здоровы: `docker-compose ps`
3. Проверьте, что файл `sqlite/cars.db` существует

### Elasticsearch не запускается

1. Проверьте логи: `docker-compose logs elasticsearch`
2. Убедитесь, что достаточно памяти (минимум 512MB)
3. Проверьте, что порты 9200 и 9300 свободны

### Миграция не выполняется

1. Проверьте, что файл `sqlite/cars.db` доступен в контейнере:
   ```bash
   docker-compose exec backend ls -la /app/sqlite/
   ```

2. Проверьте логи миграции: `docker-compose logs backend | grep миграция`

3. Запустите миграцию вручную:
   ```bash
   docker-compose exec backend python /app/migrate_cars_from_sqlite.py
   ```

### PostgreSQL не подключается

1. Проверьте, что PostgreSQL запущен: `docker-compose ps postgres`
2. Проверьте логи: `docker-compose logs postgres`
3. Проверьте переменные окружения в backend

## Порты

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8000
- **PostgreSQL**: localhost:5432
- **Elasticsearch**: http://localhost:9200

## Сеть

Все контейнеры находятся в одной Docker сети `chat-assistant-net` и могут обращаться друг к другу по именам сервисов:

- `postgres` - PostgreSQL
- `elasticsearch` - Elasticsearch
- `backend` - Backend API
- `frontend` - Frontend


