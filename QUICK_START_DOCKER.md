# 🚀 Быстрый старт с Docker

## Требования

- Docker и Docker Compose установлены
- Файл `sqlite/cars.db` существует в корне проекта

## Запуск

```bash
# 1. Запустить все контейнеры
docker-compose up -d

# 2. Проверить статус
docker-compose ps

# 3. Просмотреть логи (опционально)
docker-compose logs -f backend
```

## Что происходит автоматически

1. ✅ PostgreSQL запускается и создает базу данных
2. ✅ Elasticsearch запускается и готов к работе
3. ✅ Backend ждет готовности сервисов
4. ✅ Backend проверяет, нужно ли заполнять PostgreSQL
5. ✅ Если PostgreSQL пуст, данные мигрируются из `sqlite/cars.db`
6. ✅ Elasticsearch индексирует данные из PostgreSQL
7. ✅ Backend запускается на порту 8000

## Доступ к сервисам

- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:8000
- **Elasticsearch**: http://localhost:9200
- **PostgreSQL**: localhost:5432

## Остановка

```bash
docker-compose down
```

## Полная переустановка (⚠️ удалит все данные!)

```bash
docker-compose down -v
docker-compose up -d
```

## Проблемы?

См. подробную документацию в `DOCKER_SETUP.md`


