# Проверка зависимостей проекта

## ✅ Основные зависимости (уже установлены)

### Веб-фреймворк
- `fastapi>=0.104.1` ✅
- `uvicorn[standard]>=0.24.0` ✅
- `python-multipart>=0.0.6` ✅

### База данных
- `sqlalchemy>=2.0.23` ✅
- `psycopg2-binary>=2.9.9` ✅ (для PostgreSQL)
- `alembic>=1.12.1` ✅ (миграции)
- `pgvector>=0.2.3` ✅ (расширение PostgreSQL для векторов)

### Валидация и настройки
- `pydantic>=2.5.0` ✅
- `pydantic-settings>=2.1.0` ✅
- `python-dotenv>=1.0.0` ✅

### Аутентификация
- `python-jose[cryptography]>=3.3.0` ✅
- `passlib[bcrypt]>=1.7.4` ✅

### Поиск и векторы
- `elasticsearch==8.11.0` ✅
- `sentence-transformers>=2.2.2` ✅ (для HuggingFace эмбеддингов)
- `torch>=2.0.0` ✅ (зависимость для sentence-transformers)
- `numpy>=1.24.0,<2.0.0` ✅

### LangChain
- `langchain>=0.1.0` ✅
- `langchain-community>=0.0.20` ✅ (содержит PGVector)
- `langchain-core>=0.1.0` ✅
- `langchain-ollama>=0.1.0` ✅
- `langchain-mistralai>=0.1.0` ✅
- `langchain-openai>=0.1.0` ✅
- `langchain-anthropic>=0.1.0` ✅

### HTTP клиенты
- `httpx>=0.25.2` ✅ (для Ollama и других API)

### Дополнительные утилиты
- `redis>=5.0.1` ✅
- `chromadb>=0.4.18` ✅
- `PyPDF2>=3.0.1` ✅
- `python-docx>=0.8.11` ✅
- `transliterate>=1.10.2` ✅
- `unidecode>=1.3.7` ✅
- `beautifulsoup4>=4.12.0` ✅
- `openai>=0.28.1` ✅

## 📋 Стандартные библиотеки Python (не требуют установки)

- `threading` - стандартная библиотека
- `concurrent.futures` - стандартная библиотека
- `asyncio` - стандартная библиотека
- `typing` - стандартная библиотека
- `os`, `sys`, `json`, `re`, `time` - стандартные библиотеки

## ⚠️ Опциональные зависимости (используются с try/except)

- `spacy` - для NLP (опционально, не критично)
- `transformers` - для ML моделей (опционально, не критично)

## 🔍 Проверка установки

### Для проверки установки всех зависимостей:

```bash
# В Docker контейнере
docker-compose exec backend pip list | grep -E "(pgvector|langchain|sentence-transformers|torch|httpx)"
```

### Для установки недостающих зависимостей:

```bash
# Пересборка контейнера с новыми зависимостями
docker-compose build backend
docker-compose up -d backend
```

### Для ручной установки в контейнере:

```bash
docker-compose exec backend pip install pgvector>=0.2.3 torch>=2.0.0
```

## ✅ Все необходимые зависимости присутствуют в requirements.txt

Все модули, используемые в коде, либо:
1. Уже указаны в `requirements.txt`
2. Являются стандартными библиотеками Python
3. Импортируются с обработкой ошибок (try/except) и являются опциональными

## 🚀 Рекомендации

1. **Пересобрать Docker контейнер** после добавления `pgvector` и `torch`:
   ```bash
   docker-compose build backend
   docker-compose up -d backend
   ```

2. **Проверить установку pgvector** в PostgreSQL:
   ```sql
   CREATE EXTENSION IF NOT EXISTS vector;
   ```

3. **Проверить доступность Ollama** (если используется):
   - Убедиться, что Ollama запущен
   - Проверить доступность по адресу из настроек

