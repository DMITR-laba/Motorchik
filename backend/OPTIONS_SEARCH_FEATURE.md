# Добавлен поиск по опциям автомобилей в SQL-агент

## ✅ РЕАЛИЗОВАНО

### 1. Информация об опциях в схеме БД
- Добавлена таблица `car_options` в список обрабатываемых таблиц
- Добавлена таблица `car_options_groups` для групп опций
- Добавлены примеры опций и групп опций в схему БД

### 2. Примеры опций в схеме
- Получение примеров описаний опций (до 15 примеров)
- Получение примеров групп опций (до 10 примеров)
- Автоматическое отображение в схеме для LLM

### 3. Правила поиска по опциям в промпте
Добавлен раздел "7. ПОИСК ПО ОПЦИЯМ АВТОМОБИЛЕЙ" с инструкциями:
- Описание структуры таблиц `car_options` и `car_options_groups`
- Примеры правильных JOIN запросов
- Поиск по одной опции
- Поиск по нескольким опциям
- Поиск по группам опций
- Важные замечания о том, что опции есть только для новых автомобилей

### 4. Примеры SQL запросов
Добавлены 3 примера корректных SQL запросов:
- Поиск автомобилей с ABS (антиблокировочная система)
- Поиск автомобилей с круиз-контролем
- Поиск автомобилей с несколькими опциями (кожаный салон + подогрев сидений)

## 📋 СТРУКТУРА ДАННЫХ

### Таблица `car_options`:
- `id` - уникальный идентификатор
- `car_id` - связь с автомобилем из таблицы `cars` (INDEXED)
- `code` - код опции
- `description` - описание опции (TEXT) - основное поле для поиска
- `options_group_id` - связь с группой опций

### Таблица `car_options_groups`:
- `id` - уникальный идентификатор
- `car_id` - связь с автомобилем
- `code` - код группы
- `name` - название группы (например: "Безопасность", "Внешний вид")

## 🔍 ТИПЫ ЗАПРОСОВ

### 1. Поиск по одной опции:
```sql
SELECT DISTINCT c.id, c.mark, c.model, c.price
FROM cars c
INNER JOIN car_options co ON c.id = co.car_id
WHERE LOWER(co.description) LIKE '%круиз-контроль%'
AND c.price IS NOT NULL AND c.price != '';
```

### 2. Поиск по нескольким опциям:
```sql
SELECT DISTINCT c.id, c.mark, c.model, c.price
FROM cars c
WHERE c.id IN (SELECT car_id FROM car_options WHERE LOWER(description) LIKE '%кожаный салон%')
AND c.id IN (SELECT car_id FROM car_options WHERE LOWER(description) LIKE '%подогрев сидений%')
AND c.price IS NOT NULL AND c.price != '';
```

### 3. Поиск по группе опций:
```sql
SELECT DISTINCT c.id, c.mark, c.model, c.price
FROM cars c
INNER JOIN car_options co ON c.id = co.car_id
INNER JOIN car_options_groups cog ON co.options_group_id = cog.id
WHERE LOWER(cog.name) LIKE '%безопасность%'
AND c.price IS NOT NULL AND c.price != '';
```

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Опции есть только для новых автомобилей** (таблица `cars`)
2. **Подержанные автомобили** (`used_cars`) **НЕ имеют опций** в таблице `car_options`
3. **Используй LOWER()** для регистронезависимого поиска
4. **Используй LIKE '%текст%'** для поиска по части описания
5. **Используй DISTINCT** чтобы избежать дублирования при JOIN

## 🎯 ПРИМЕРЫ ЗАПРОСОВ, КОТОРЫЕ ТЕПЕРЬ ПОДДЕРЖИВАЮТСЯ

- "Найди автомобили с ABS"
- "Покажи автомобили с круиз-контролем"
- "Найди автомобили с кожаным салоном и подогревом сидений"
- "Покажи автомобили с опциями безопасности"
- "Найди Toyota с кожаным салоном"
- "Сколько автомобилей с парктроником?"

## ✅ ГОТОВНОСТЬ

SQL-агент теперь поддерживает поиск по опциям! Все изменения внесены:
- ✅ Схема БД включает информацию об опциях
- ✅ Промпт содержит правила поиска по опциям
- ✅ Примеры SQL запросов добавлены
- ✅ Описания полей обновлены




