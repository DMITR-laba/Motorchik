# Информация о SSL сертификатах

## 📍 Где находятся сертификаты

Сертификаты хранятся в директории:
```
certbot/conf/live/app.domain/
```

Файлы:
- `fullchain.pem` - сертификат (1956 байт)
- `privkey.pem` - приватный ключ (3272 байт)

Эти файлы монтируются в Nginx контейнер по пути:
```
/etc/letsencrypt/live/app.domain/
```

## ⏰ Когда создаются сертификаты

### Текущая ситуация (самоподписанные сертификаты)

**Сертификаты создаются ВРУЧНУЮ**, не автоматически при запуске контейнеров.

Сертификаты были созданы ранее через Docker контейнер:
```bash
docker run --rm -v "${PWD}/certbot/conf:/etc/letsencrypt" alpine/openssl req -x509 -nodes -newkey rsa:4096 -days 365 -keyout /etc/letsencrypt/live/app.domain/privkey.pem -out /etc/letsencrypt/live/app.domain/fullchain.pem -subj "/CN=app.domain/O=Motorchik/C=RU" -addext "subjectAltName=DNS:app.domain,DNS:*.app.domain,IP:127.0.0.1"
```

**Дата создания:** 09.11.2025 22:52:09

## 🔧 Как создать сертификаты

### Вариант 1: Через Docker контейнер (рекомендуется)

```bash
docker run --rm -v "${PWD}/certbot/conf:/certs" alpine/openssl sh -c "mkdir -p /certs/live/app.domain && openssl req -x509 -nodes -newkey rsa:4096 -days 365 -keyout /certs/live/app.domain/privkey.pem -out /certs/live/app.domain/fullchain.pem -subj '/CN=app.domain/O=Motorchik/C=RU' -addext 'subjectAltName=DNS:app.domain,DNS:*.app.domain,IP:127.0.0.1'"
```

### Вариант 2: Через скрипт в Nginx контейнере

```bash
docker-compose exec nginx sh /usr/local/bin/generate-self-signed-cert.sh
```

### Вариант 3: Через PowerShell скрипт (Windows)

```powershell
.\create-cert.ps1
```

## 📋 Конфигурация в docker-compose.yml

Сертификаты монтируются в Nginx контейнер:

```yaml
nginx:
  volumes:
    - ./certbot/conf:/etc/letsencrypt  # Сертификаты
    - ./certbot/www:/var/www/certbot   # Для Let's Encrypt challenges
```

## 🔐 Типы сертификатов

### 1. Самоподписанные (текущие, для тестирования)

- **Создаются:** Вручную
- **Срок действия:** 365 дней
- **Использование:** Локальное тестирование
- **Особенности:** Браузер показывает предупреждение о безопасности

### 2. Let's Encrypt (для продакшена)

- **Создаются:** Автоматически через certbot
- **Срок действия:** 90 дней (автоматически обновляется)
- **Использование:** Продакшен
- **Особенности:** Браузеры доверяют сертификату

## 🔄 Автоматическое обновление

### Для самоподписанных сертификатов

**НЕ обновляются автоматически.** Нужно создавать вручную при истечении срока.

### Для Let's Encrypt

Контейнер `certbot` автоматически обновляет сертификаты каждые 12 часов:

```yaml
certbot:
  entrypoint: "/bin/sh -c 'trap exit TERM; while :; do certbot renew; sleep 12h & wait $${!}; done;'"
```

## 📝 Проверка сертификатов

### Проверить наличие сертификатов:

```bash
# На хосте
ls -lh certbot/conf/live/app.domain/

# В контейнере Nginx
docker-compose exec nginx ls -lh /etc/letsencrypt/live/app.domain/
```

### Проверить срок действия:

```bash
docker-compose exec nginx openssl x509 -in /etc/letsencrypt/live/app.domain/fullchain.pem -noout -dates
```

### Проверить информацию о сертификате:

```bash
docker-compose exec nginx openssl x509 -in /etc/letsencrypt/live/app.domain/fullchain.pem -noout -text
```

## ⚠️ Важно

1. **Самоподписанные сертификаты** создаются вручную и не обновляются автоматически
2. **Let's Encrypt сертификаты** требуют настройки DNS и создаются через certbot
3. При изменении домена нужно **пересоздать сертификаты** для нового домена
4. Сертификаты должны быть созданы **ДО** запуска Nginx с HTTPS конфигурацией

## 🔄 Процесс создания при первом запуске

Текущая конфигурация **НЕ создает сертификаты автоматически**. 

Для автоматического создания можно добавить в `docker-entrypoint.sh` Nginx или создать init-скрипт, который проверяет наличие сертификатов и создает их при отсутствии.

