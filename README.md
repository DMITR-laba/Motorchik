# Motorchik - AI-Помощник по подбору автомобилей

Система для подбора автомобилей с использованием AI-ассистента, SQL-агента и Elasticsearch для поиска.

## 🚀 Быстрый старт

### Клонирование репозитория

```bash
git clone https://github.com/DMITR-laba/Motorchik.git
cd Motorchik
```

### Обновление проекта после клонирования

Если вы уже клонировали проект ранее и хотите получить последние изменения:

```bash
# Перейдите в директорию проекта
cd Motorchik

# Получите последние изменения из репозитория
git pull origin main

# Если возникли конфликты, используйте:
git fetch origin
git merge origin/main

# Или для принудительного обновления (⚠️ перезапишет локальные изменения):
git fetch origin
git reset --hard origin/main
```

### Первоначальная настройка

1. **Создайте файл `.env`** на основе `env.example`:
   ```bash
   cp env.example .env
   ```

2. **Настройте переменные окружения** в `.env`:
   - `OLLAMA_HOST=host.docker.internal` (без http://)
   - `OLLAMA_PORT=11434`
   - `OLLAMA_MODEL=llama3:8b`
   - И другие необходимые настройки

3. **Убедитесь, что файл `sqlite/cars.db` существует** (для миграции данных)

### Запуск через Docker Compose

```bash
# Сборка и запуск всех контейнеров
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down

# Остановка с удалением volumes (⚠️ удалит данные)
docker-compose down -v
```

## 📋 Структура проекта

```
Motorchik/
├── backend/              # Backend API (FastAPI)
├── frontend/             # Frontend интерфейс
├── nginx/                # Nginx конфигурация
├── certbot/              # SSL сертификаты
├── sqlite/               # SQLite база данных (для миграции)
├── docker-compose.yml    # Docker Compose конфигурация
└── .env                  # Переменные окружения
```

## 🔧 Основные компоненты

- **Backend**: FastAPI приложение с SQL-агентом и Elasticsearch
- **Frontend**: Веб-интерфейс с админ-панелью
- **PostgreSQL**: Основная база данных
- **Elasticsearch**: Поисковый движок
- **Nginx**: Reverse proxy с SSL
- **Ollama**: Локальная LLM для генерации ответов

## 🌐 Настройка домена

### Локальное использование

1. Откройте админ-панель → Настройка домена
2. Введите доменное имя (например: `app.domain`)
3. Нажмите "Сохранить домен"
4. SSL сертификат создастся автоматически

### Настройка hosts файла (для локального доступа)

**Windows:**
```
1. Откройте: C:\Windows\System32\drivers\etc\hosts (от имени администратора)
2. Добавьте: 127.0.0.1    app.domain
3. Очистите DNS кэш: ipconfig /flushdns
```

**Linux/Mac:**
```bash
sudo nano /etc/hosts
# Добавьте: 127.0.0.1    app.domain
```

### Доступ к приложению

- HTTP: `http://localhost` или `http://app.domain`
- HTTPS: `https://localhost` или `https://app.domain`

## 🔐 Вход в систему

По умолчанию создается администратор:
- **Email**: `admin@example.com`
- **Пароль**: `admin123`

## 📚 Дополнительная документация

- `NGINX_SSL_SETUP.md` - Настройка SSL с Let's Encrypt
- `SSL_CERTIFICATES_INFO.md` - Информация о SSL сертификатах
- `DOMAIN_SETUP_GUIDE.md` - Руководство по настройке домена
- `AUTO_CERT_CREATION.md` - Автоматическое создание сертификатов

## 🔄 Обновление проекта

### Регулярное обновление

```bash
# Получить последние изменения
git pull origin main

# Пересобрать контейнеры (если были изменения в коде)
docker-compose build

# Перезапустить контейнеры
docker-compose up -d
```

### После обновления

1. Проверьте, не изменились ли переменные окружения в `.env`
2. Обновите `requirements.txt` если нужно:
   ```bash
   docker-compose exec motorchik-backend pip install -r requirements.txt
   ```
3. Перезапустите контейнеры:
   ```bash
   docker-compose restart
   ```

## 🛠️ Разработка

### Локальная разработка

```bash
# Backend
cd backend
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
pip install -r requirements.txt
uvicorn main:app --reload
```

### Тестирование

```bash
# Проверка здоровья сервисов
curl http://localhost/api/health

# Проверка API документации
open http://localhost/api/docs
```

## 📝 Миграция данных

При первом запуске автоматически выполняется миграция из SQLite в PostgreSQL:
- Файл `sqlite/cars.db` должен находиться в директории `sqlite/`
- Миграция выполняется автоматически при старте backend контейнера

## ⚠️ Важные замечания

1. **SSL сертификаты**: По умолчанию создаются самоподписанные сертификаты для тестирования
2. **Ollama**: Должен быть запущен локально на порту 11434
3. **Данные**: PostgreSQL и Elasticsearch данные хранятся в Docker volumes
4. **Безопасность**: Для продакшена измените пароли и используйте Let's Encrypt

## 🐛 Решение проблем

### Проблемы с подключением

```bash
# Проверка статуса контейнеров
docker-compose ps

# Просмотр логов
docker-compose logs motorchik-backend
docker-compose logs nginx

# Перезапуск сервиса
docker-compose restart motorchik-backend
```

### Проблемы с SSL

```bash
# Пересоздание сертификата
docker-compose exec nginx sh /usr/local/bin/generate-self-signed-cert.sh
```

### Проблемы с базой данных

```bash
# Проверка подключения к PostgreSQL
docker-compose exec motorchik-postgres psql -U postgres -d vectordb -c "SELECT 1;"
```

## 📞 Поддержка

Для вопросов и проблем создайте issue в репозитории.

## 📄 Лицензия

[Укажите лицензию]
